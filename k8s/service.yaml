apiVersion: v1
kind: Service
metadata:
  name: python-app
spec:
  selector:
    app: python-app
  ports:
  - protocol: TCP
    port: 8080       # Service port (what clients use inside the cluster)
    targetPort: 5000  # Container port on the pod
  type: ClusterIP   # Default (can be omitted)
